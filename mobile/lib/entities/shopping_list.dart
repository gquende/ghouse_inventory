class ShoppingList {
  String description;
  String categoryUUID;
  int statusId;

  ShoppingList(
      {required this.description,
      required this.categoryUUID,
      required this.statusId});
}
